---
const { name, techs, img, github, demo } = Astro.props;
---

<li
  class="border border-teal-600 max-w-xl p-3 flex flex-col justify-center items-center"
>
  {img && <img src={img.url} alt={img.alt} />}
  <h3 class="text-2xl text-teal-600 my-2">{name}</h3>
  <ul class="flex flex-wrap justify-center items-center my-2">
    {techs.map((tech) => <li class="bg-teal-500 mt-1 ml-1 px-1">{tech}</li>)}
  </ul>
  <div class="flex items-end h-full">
    <a
      href=`http://github/BassamXYZ/${github}`
      class="mx-3 p-1 text-teal-600 hover:text-blue-600">Github</a
    >
    {demo && <a href=`${demo}` class="mx-3 p-1 text-teal-600 hover:text-blue-600">Demo</a>}
  </div>
</li>
